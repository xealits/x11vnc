cmake_minimum_required(VERSION 3.28)
project(x11vncpp VERSION 0.0.1)
set(CXX_STANDARD 23)

#add_compile_definitions(VERSION="${PROJECT_VERSION}") # currently config.h contains VERSION
# Autogenerated by auto2cmake on 2025-01-19 22:17:15

# Options

# Option to Enable 
option(  "Enable " OFF )

# Option to Enable BUILD_X11VNC
option( BUILD_X11VNC "Enable BUILD_X11VNC" OFF )

# Option to Enable HAVE_ANDROID
option( HAVE_ANDROID "Enable HAVE_ANDROID" OFF )

# Option to Enable HAVE_CYGIPC
option( HAVE_CYGIPC "Enable HAVE_CYGIPC" OFF )

# Option to Enable HAVE_MACOSX_OPENGL_H
option( HAVE_MACOSX_OPENGL_H "Enable HAVE_MACOSX_OPENGL_H" OFF )

# Option to Enable HAVE_OSX
option( HAVE_OSX "Enable HAVE_OSX" OFF )

# Option to Enable HAVE_X11
option( HAVE_X11 "Enable HAVE_X11" OFF )

# Option to Enable HAVE_XI2
option( HAVE_XI2 "Enable HAVE_XI2" OFF )

# Option to Enable MINGW
option( MINGW "Enable MINGW" OFF )

# Option to Enable RPMSOURCEDIR
option( RPMSOURCEDIR "Enable RPMSOURCEDIR" OFF )

# Option to Enable SSL_LIBS
option( SSL_LIBS "Enable SSL_LIBS" OFF )

# Option to Enable WITH_CAIRO
option( WITH_CAIRO "Enable WITH_CAIRO" OFF )

# The lines below will generate the config.h based on the options above
# The file will be in the ${CMAKE_BINARY_DIR} location
set(CONFIG_H ${CMAKE_BINARY_DIR}/config.h)
# TODO: somewhere here it copies config.h to the binary directory, which is not needed!
string(TIMESTAMP CURRENT_TIMESTAMP)
file(WRITE ${CONFIG_H} "/* WARNING: This file is auto-generated by CMake on ${CURRENT_TIMESTAMP}. DO NOT EDIT!!! */\n\n")
if(  )
    message("  Enabled")
    file(APPEND ${CONFIG_H} "/* Enable */\n")
    file(APPEND ${CONFIG_H} "#define ENOENT_WORKAROUND \n\n")
## !!! WARNING HAVE_MACOSX_NATIVE_DISPLAY Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_MACOSX_NATIVE_DISPLAY\n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
## !!! WARNING NEED_INADDR_T Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define NEED_INADDR_T\n\n")
endif(  )
if( BUILD_X11VNC )
    message(" BUILD_X11VNC Enabled")
    file(APPEND ${CONFIG_H} "/* Enable BUILD_X11VNC */\n")
    file(APPEND ${CONFIG_H} "#define WITH_X11VNC \n\n")
endif( BUILD_X11VNC )
if( HAVE_ANDROID )
    message(" HAVE_ANDROID Enabled")
    file(APPEND ${CONFIG_H} "/* Android host system detected */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_ANDROID 1\n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
endif( HAVE_ANDROID )
if( HAVE_CYGIPC )
    message(" HAVE_CYGIPC Enabled")
    file(APPEND ${CONFIG_H} "/* Enable HAVE_CYGIPC */\n")
    file(APPEND ${CONFIG_H} "#define CYGIPC \n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
endif( HAVE_CYGIPC )
if( HAVE_MACOSX_OPENGL_H )
    message(" HAVE_MACOSX_OPENGL_H Enabled")
    file(APPEND ${CONFIG_H} "/* Enable HAVE_MACOSX_OPENGL_H */\n")
    file(APPEND ${CONFIG_H} "#define OSX_OPENGL \n\n")
## !!! WARNING HAVE_MACOSX_NATIVE_DISPLAY Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_MACOSX_NATIVE_DISPLAY\n\n")
endif( HAVE_MACOSX_OPENGL_H )
if( HAVE_OSX )
    message(" HAVE_OSX Enabled")
    file(APPEND ${CONFIG_H} "/* Enable HAVE_OSX */\n")
    file(APPEND ${CONFIG_H} "#define OSX \n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
endif( HAVE_OSX )
if( HAVE_X11 )
    message(" HAVE_X11 Enabled")
    file(APPEND ${CONFIG_H} "/* Enable HAVE_X11 */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_X11 \n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
endif( HAVE_X11 )
if( HAVE_XI2 )
    message(" HAVE_XI2 Enabled")
    file(APPEND ${CONFIG_H} "/* Enable HAVE_XI2 */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_XI2 \n\n")
## !!! WARNING HAVE_AVAHI Identified with some pattern matching magic.
## Remove if not relevant!")
    file(APPEND ${CONFIG_H} "#define HAVE_AVAHI\n\n")
endif( HAVE_XI2 )
if( MINGW )
    message(" MINGW Enabled")
    file(APPEND ${CONFIG_H} "/* Enable MINGW */\n")
    file(APPEND ${CONFIG_H} "#define MINGW \n\n")
endif( MINGW )
if( RPMSOURCEDIR )
    message(" RPMSOURCEDIR Enabled")
    file(APPEND ${CONFIG_H} "/* Enable RPMSOURCEDIR */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_RPM \n\n")
endif( RPMSOURCEDIR )
if( SSL_LIBS )
    message(" SSL_LIBS Enabled")
    file(APPEND ${CONFIG_H} "/* Enable SSL_LIBS */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_LIBSSL \n\n")
endif( SSL_LIBS )
if( WITH_CAIRO )
    message(" WITH_CAIRO Enabled")
    file(APPEND ${CONFIG_H} "/* Enable WITH_CAIRO */\n")
    file(APPEND ${CONFIG_H} "#define HAVE_CAIRO \n\n")
endif( WITH_CAIRO )

## !!! WARNING These are the defines that were defined regardless of an option.
## !!! Or the script couldn't match them. Match them accordingly, delete them or keep them

# Setting the include directory for the application to find config.h
#include_directories( ${CMAKE_BINARY_DIR}/ ) # it copied an empty config.h here, which messes up everything
include_directories( ${CMAKE_SOURCE_DIR}/ ) # the directory with config.h

# Since we have created a config.h add a global define for it
add_definitions( "-DHAVE_CONFIG_H" )
add_subdirectory( src )
add_subdirectory( misc )
